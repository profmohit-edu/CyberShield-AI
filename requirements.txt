# Runtime dependencies
fastapi==0.139.2
uvicorn[standard]==0.51.0
Jinja2==3.1.6
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.14.2

# HTTP client used by application services and FastAPI integration tests
httpx==0.28.1

# Test dependencies
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0

# Static analysis and formatting
ruff==0.15.22
mypy==2.3.0

# Slither, Mythril, and Solhint are executed as isolated security-engine
# workers. Their versions are pinned in the corresponding container
# definitions to prevent incompatible analyzer dependency graphs from
# entering the FastAPI application environment.
